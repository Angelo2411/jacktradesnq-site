10-year backtest of ISM Manufacturing PMI at 10:00 ET on GC futures, using the IFVG entry pattern with SI (Silver) SMT confirmation.

## Model

Same engine as the NQ variant (IFVG inversion + opposite-leg sweep filter + "TP1 + BE" variant), executed on GC. Release time is 10:00 ET (ISM, first business day of month); sweep window extends to 12:00 ET, resolve deadline 16:00 ET.

## SMT pair

NQ variants use ES as the SMT confirmation pair. GC variants use SI (Silver) — the closest correlated metals contract.

## Why This Event

ISM Manufacturing is a leading macro indicator. Precious metals react to manufacturing prints via the USD channel — a weak reading typically pressures the dollar and supports gold, generating a directional sweep that SI can confirm or deny.

## Results

See the stat band above and the By weekday / By year / Trade list tabs for full breakdown.
